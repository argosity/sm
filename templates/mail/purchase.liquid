<html>
  <body>
    <table>
      <tr>
        <td>
          <p style="font-size: 120%;">
            Hello {{ purchase.name }},
          </p>
          <p>
            Thanks purchasing tickets to {{ show.title }}.
          </p>
          <p>
            Your receipt ID is {{ purchase.identifier }}.
            Please mention that number in correspondence with us so we can help you faster.
          </p>
          <p>
            <a href="{{ purchase.tickets_url }}">Download Tickets</a>
          </p>
          <p style="margin-top: 20px;">
            {{ company_name }}
          </p>
        </td>
        <td>
          {% if presenter %}
          <h3>
            {% if presenter.logo %}
            <img src="{{ presenter.logo.url }}" width="{{ presenter.logo.width }}" height="{{ presenter.logo.height }}">
            {% else %}
            {{ presenter.name }}
            {% endif %}
            Presents:
          </h3>
          <br>
          {% endif %}
          <h1>{{show.title}}</h1>
          {% if show.image %}
          <img src="{{ show.image.url }}" width="{{ show.image.width }}" height="{{ show.image.height }}">
          {% endif %}
        </td>
      </tr>
    </table>
  </body>
</html>
